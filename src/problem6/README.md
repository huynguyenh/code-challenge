# Problem 6 — Architecture (deferred)

This problem is **documentation only** — write the spec for a backend module that powers a live top-10 scoreboard, with execution-flow diagram and notes on preventing unauthorised score writes.

It is intentionally not addressed in this submission round. The folder is kept here so the repo layout mirrors the skeletal `99techteam/code-challenge` repo.
